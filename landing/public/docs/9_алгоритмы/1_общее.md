# 🧠 Алгоритмы — основы для сеньора

Алгоритм — это **конкретная последовательность шагов для решения задачи**. В контексте собеса важно не только знать реализацию, но и понимать **сложность, когда применять, trade-offs**.

---

## 1️⃣ **Типы алгоритмов по способу решения**

### 1.1 **Жадные (Greedy)**

* На каждом шаге выбираем **локально оптимальное решение**, надеясь, что получится глобально оптимальное.
* Применение: задачи выбора минимальной стоимости, покрытие множества.

**Пример:**

* Алгоритм Краскала для MST (Minimum Spanning Tree)

```text
1. Сортируем рёбра по весу
2. Берём наименьшее ребро, не создавая цикл
3. Повторяем, пока все вершины не соединены
```

**Сложность:** O(E log E)

---

### 1.2 **Разделяй и властвуй (Divide & Conquer)**

* Делим задачу на подзадачи, решаем их рекурсивно, объединяем результат.
* Примеры: QuickSort, MergeSort, Binary Search, FFT

**Пример MergeSort:**

```
mergeSort(array):
  if length <= 1 → return
  mid = length / 2
  left = mergeSort(array[0..mid])
  right = mergeSort(array[mid..end])
  return merge(left, right)
```

* **Сложность:** O(n log n)
* **Плюс:** стабильный, предсказуемый
* **Минус:** рекурсия + дополнительная память для слияния

---

### 1.3 **Динамическое программирование (DP)**

* Разбиваем задачу на **подзадачи**, сохраняем результат (мемоизация), чтобы не пересчитывать.
* Применение: оптимизация, задачи на пути, рюкзак, подсчёт комбинаторики

**Пример: Fibonacci с мемоизацией**

```js
function fib(n, memo = {}) {
  if (n <= 1) return n;
  if (memo[n]) return memo[n];
  memo[n] = fib(n-1, memo) + fib(n-2, memo);
  return memo[n];
}
```

* **Сложность:** O(n) вместо O(2^n) рекурсии

---

### 1.4 **Backtracking**

* Пробуем все варианты, но **отсекаем явно невозможные решения**.
* Применение: N-Queens, Sudoku, комбинаторные задачи

**Пример:** N-Queens

```
placeQueen(row):
  if row == N → solution found
  for col in 0..N-1:
    if valid(row, col):
      placeQueen(row+1)
```

---

### 1.5 **Поиск и обход графов**

* BFS (Breadth-First Search) → поиск в ширину, shortest path unweighted
* DFS (Depth-First Search) → поиск в глубину, topological sort, backtracking
* Dijkstra → кратчайший путь, non-negative weights
* Bellman-Ford → кратчайший путь, negative weights
* Floyd-Warshall → все пары кратчайших путей

---

## 2️⃣ **Основные структуры данных и их роль**

| Структура             | Применение                             |
| --------------------- | -------------------------------------- |
| Array                 | хранение последовательных данных       |
| Linked List           | частые вставки/удаления                |
| Stack                 | LIFO, undo/redo, DFS                   |
| Queue                 | FIFO, BFS, асинхронные очереди         |
| Priority Queue / Heap | Dijkstra, A*, топ-N задач              |
| Hash Table            | поиск, кэш, словари                    |
| Graph                 | сети, маршруты, зависимости            |
| Tree                  | бинарный поиск, балансировка, иерархии |

---

## 3️⃣ **Анализ сложности (Big O)**

* **Время**:

  * O(1) → константное (array access, hash lookup)
  * O(log n) → бинарный поиск
  * O(n) → линейный проход
  * O(n log n) → сортировка
  * O(n^2) → вложенные циклы
* **Память**:

  * Стараться избегать лишних копий массивов (DP, recursion)

---

## 4️⃣ **Сортировка**

| Алгоритм           | Сложность                    | Особенности                   |
| ------------------ | ---------------------------- | ----------------------------- |
| QuickSort          | O(n log n) avg, O(n^2) worst | in-place, unstable            |
| MergeSort          | O(n log n)                   | stable, требует доп. памяти   |
| HeapSort           | O(n log n)                   | in-place, unstable            |
| Bubble / Insertion | O(n^2)                       | только для маленьких массивов |

---

## 5️⃣ **Поиск**

* **Binary Search** → O(log n), работает только на отсортированных данных
* **Linear Search** → O(n), универсальный, но медленный

---

## 6️⃣ **Графовые алгоритмы**

* **BFS** → кратчайший путь без веса
* **DFS** → топологическая сортировка, connected components
* **Dijkstra** → кратчайший путь с весами ≥0
* **A*** → поиск с эвристикой (например, карта)

---

## 7️⃣ **Жадные алгоритмы**

* **Краскал / Прим** → MST
* **Huffman Coding** → сжатие данных
* **Activity Selection** → задачи по времени

---

## 8️⃣ **Рекурсия vs Итерация**

* Рекурсия удобна для дерева, графа, DP
* Итерация лучше для больших массивов → экономим стек
* В Node.js важно помнить **ограничение стека** для глубоких рекурсий

---

## 9️⃣ **Типичные собесные вопросы**

* Разница BFS vs DFS
* Binary Search на отсортированном массиве
* Сортировка с O(n log n)
* Реализация priority queue / heap
* DP: рюкзак, Fibonacci, подсчёт путей
* Граф: кратчайший путь, топологическая сортировка

---

## 🔹 Ключевые принципы для сеньора

1. **Выбор структуры данных решает алгоритм**
2. **Понимание сложности критично для масштабируемых сервисов**
3. **DP и мемоизация → оптимизация рекурсии**
4. **Графы → реальные кейсы (routing, dependencies, workflow)**
5. **Greedy, Divide&Conquer, Backtracking → know when to use**
