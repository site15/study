# 🧩 Типы задачек и алгоритмические паттерны

---

## 1️⃣ **Массивы (Arrays)**

**Описание:**

* Основная структура данных, хранит последовательность элементов
* Задачи: поиск, фильтрация, слияние, ротация, подсчёт сумм

**Примеры задач:**

* Two Sum → найти два числа, которые дают заданную сумму
* Rotate Array → циклический сдвиг массива
* Maximum Subarray → найти подмассив с максимальной суммой

**Продакшен-применение:**

* Фильтрация и сортировка данных из БД
* Кэширование результатов в массивы для быстрого доступа

**Node.js пример:**

```ts
const sum = (arr: number[], target: number) => {
  const map = new Map();
  for (let i = 0; i < arr.length; i++) {
    if (map.has(target - arr[i])) return [map.get(target - arr[i]), i];
    map.set(arr[i], i);
  }
  return [];
};
```

---

## 2️⃣ **Строки (Strings)**

**Описание:**

* Массив символов с дополнительными операциями
* Задачи: поиск подстрок, реверс, палиндром, парсинг

**Примеры задач:**

* Longest Substring Without Repeating Characters
* Valid Palindrome
* Implement strStr() → поиск подстроки

**Продакшен-применение:**

* Валидация данных (email, username)
* Парсинг JSON или логов
* Автокомплит и поиск по тексту

**Node.js пример:**

```ts
const isPalindrome = (s: string) => {
  s = s.replace(/[^a-z0-9]/gi, '').toLowerCase();
  return s === s.split('').reverse().join('');
};
```

---

## 3️⃣ **Стеки и очереди (Stacks & Queues)**

**Описание:**

* **Стек** → LIFO (последний вошёл — первый вышел)
* **Очередь** → FIFO (первый вошёл — первый вышел)

**Примеры задач:**

* Valid Parentheses (стек)
* Min Stack (поддержка минимального элемента)
* Sliding Window Maximum (deque/queue)

**Продакшен-применение:**

* Undo/redo в UI
* Очереди задач (RabbitMQ/BullMQ)
* Алгоритмы обработки потоков

**Node.js пример:**

```ts
const validParentheses = (s: string) => {
  const stack: string[] = [];
  const map = { '(': ')', '[': ']', '{': '}' };
  for (const char of s) {
    if (map[char]) stack.push(char);
    else if (stack.pop() !== Object.keys(map).find(k => map[k] === char)) return false;
  }
  return stack.length === 0;
};
```

---

## 4️⃣ **Хэш-таблицы (Hash Maps / Sets)**

**Описание:**

* Быстрый поиск, вставка, удаление → O(1)
* Часто используются для подсчёта, уникальности, мемоизации

**Примеры задач:**

* Two Sum (Map)
* Group Anagrams
* Subarray Sum Equals K

**Продакшен-применение:**

* Кэширование (Map/Redis)
* Быстрый поиск по ключу
* Подсчёт повторов, лимитов

**Node.js пример:**

```ts
const groupAnagrams = (words: string[]) => {
  const map = new Map();
  for (const word of words) {
    const key = word.split('').sort().join('');
    if (!map.has(key)) map.set(key, []);
    map.get(key).push(word);
  }
  return Array.from(map.values());
};
```

---

## 5️⃣ **Деревья (Trees / Binary Trees / BST)**

**Описание:**

* Иерархическая структура, вершины (nodes) с потомками (children)
* Binary Tree → 2 ребёнка
* BST → отсортированное дерево → быстрый поиск

**Примеры задач:**

* Binary Tree Traversal (inorder, preorder, postorder)
* Lowest Common Ancestor
* Validate Binary Search Tree

**Продакшен-применение:**

* Категории продуктов / вложенные меню
* DOM / AST (анализ кода)
* Быстрый поиск, range queries

**Node.js пример:**

```ts
class TreeNode {
  constructor(public val: number, public left: TreeNode = null, public right: TreeNode = null) {}
}

const inorder = (root: TreeNode): number[] => {
  if (!root) return [];
  return [...inorder(root.left), root.val, ...inorder(root.right)];
};
```

---

## 6️⃣ **Графы (Graphs)**

**Описание:**

* Набор узлов (vertices) и связей (edges)
* Направленные / ненаправленные, взвешенные / невзвешенные

**Примеры задач:**

* BFS / DFS → поиск пути, connected components
* Dijkstra → кратчайший путь
* Topological Sort → зависимости

**Продакшен-применение:**

* Соцсети → друзья / подписчики
* Workflow → зависимости задач
* Routing / навигация

---

## 7️⃣ **Кучи (Heap / Priority Queue)**

**Описание:**

* Специальная структура для хранения min/max
* Позволяет быстро брать минимальный или максимальный элемент

**Примеры задач:**

* Top K Frequent Elements
* Merge K Sorted Lists

**Продакшен-применение:**

* Планирование задач по приоритету
* Лимит на топ-N
* Очереди с приоритетами

---

## 8️⃣ **Динамическое программирование (DP)**

**Описание:**

* Разбиваем задачу на подзадачи, сохраняем промежуточные результаты
* Используется для **оптимизации рекурсивных задач**

**Примеры задач:**

* Fibonacci с мемоизацией
* Knapsack / Рюкзак
* Unique Paths / Grid DP

**Продакшен-применение:**

* Оптимизация планирования
* Прогнозирование
* Оптимальные маршруты

---

## 9️⃣ **Двойные указатели / Sliding Window**

**Описание:**

* Два указателя на массив → эффективно решает задачи поиска подмассива / substring

**Примеры задач:**

* Longest Substring Without Repeating Characters
* Container With Most Water
* Sliding Window Maximum

**Продакшен-применение:**

* Потоковая обработка данных
* Лимиты на API / rate limiting
* Мониторинг метрик

---

## 🔟 **Комбинаторика / Backtracking**

**Описание:**

* Пробуем все варианты, отбрасываем невозможные → все перестановки / комбинации

**Примеры задач:**

* N-Queens
* Sudoku Solver
* Generate Parentheses

**Продакшен-применение:**

* Генераторы конфигураций
* Планирование задач
* Тестовые данные / edge case генерация
