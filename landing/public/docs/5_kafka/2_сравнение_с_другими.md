# 🧩 Сравнение потоковых брокеров сообщений

| Характеристика                  | **Kafka**                                                | **RabbitMQ**                                       | **Redis Streams**                                      | **NATS Streaming / JetStream**                                     |
| ------------------------------- | -------------------------------------------------------- | -------------------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------ |
| **Модель**                      | Log-based, immutable events                              | Queue-based, pub-sub                               | Log-based / append-only                                | Log-based, pub-sub + queue groups                                  |
| **Хранение**                    | Долгое, configurable retention                           | Пока не ack / durable queue                        | Долго (по длине + maxlen), в памяти + disk             | Долго (retention + ack), disk-backed                               |
| **Гарантия доставки**           | at-most-once / at-least-once / exactly-once              | at-least-once                                      | at-least-once                                          | at-least-once / exactly-once                                       |
| **Масштабирование**             | Партиции + brokers, горизонтально                        | Очереди + воркеры, горизонтально сложно            | Шардинг Redis, сложнее                                 | Каналы + кластеринг, горизонтально                                 |
| **Скорость / latency**          | низкая миллисекунды, high throughput                     | миллисекунды, medium throughput                    | очень быстро (in-memory), latency <1ms                 | очень быстро, миллисекунды, lightweight                            |
| **Ordering**                    | гарантировано в пределах партиции                        | FIFO на очереди                                    | FIFO в stream                                          | FIFO в channel, per subscriber                                     |
| **Persistence**                 | disk-backed, репликация                                  | disk-backed / memory                               | memory + optional disk                                 | disk-backed, replication optional                                  |
| **Backpressure handling**       | consumer lag, flow-control через offset commit           | prefetch, ack, drain                               | maxlen, blocking read                                  | acks, flow-control, rate-limiting                                  |
| **Use-cases**                   | Event sourcing, analytics, stream processing, audit logs | Background tasks, pub-sub, workflow, notifications | High-speed queueing, ephemeral data, counters, streams | Microservices async messaging, lightweight event bus, IoT, pub-sub |
| **Complexity**                  | Высокая, требует zookeeper / kafka cluster               | Средняя, простой setup                             | Низкая, Redis уже в продакшене                         | Низкая/средняя, lightweight, простой кластер                       |
| **Transactions / Exactly-once** | Да, через transactional producer/consumer                | Нет (at-least-once)                                | Нет (at-least-once)                                    | Да (ACK + durable messages)                                        |
| **Replay / Event Sourcing**     | Да, offset → replay                                      | Нет / только DLX                                   | Ограничено maxlen                                      | Да, replay через offset                                            |

---

# 🔹 Когда что использовать (типичные сценарии)

### **Kafka**

* Миллионы сообщений в секунду
* Event sourcing, аналитика, stream processing
* Long-term retention, audit trails

### **RabbitMQ**

* Background jobs, task queues, async workflows
* Pub-sub для микросервисов
* Когда важна сложная маршрутизация (direct, topic, fanout)

### **Redis Streams**

* Очень быстрые очереди с low-latency
* IoT, counters, ephemeral data, short-lived pipelines
* Лёгкая интеграция с существующим Redis stack

### **NATS Streaming / JetStream**

* Лёгкий event bus для микросервисов
* IoT, pub-sub, message broadcasting
* Exactly-once при минимальной конфигурации
* Отлично для real-time low-latency

---

# 🔹 Отличия в обработке “тяжелых сценариев”

| Сценарий                      | Kafka               | RabbitMQ           | Redis Streams     | NATS Streaming                  |
| ----------------------------- | ------------------- | ------------------ | ----------------- | ------------------------------- |
| Высокая нагрузка (миллионы/с) | 👍 партиции         | ⚠️ сложно          | ⚠️ зависит от RAM | 👍 лёгкий, масштабируемый       |
| Событийная история / replay   | 👍 offset-based     | ❌ нет              | ⚠️ ограничена     | 👍 по offset / durable messages |
| Асинхронная очередь задач     | ⚠️ overkill         | 👍                 | 👍                | 👍                              |
| Pub-sub                       | 👍 через topics     | 👍 через exchanges | 👍                | 👍, lightweight                 |
| Exactly-once                  | 👍 через транзакции | ❌                  | ❌                 | 👍 (ACK + durable)              |
| Простота setup                | ⚠️ сложнее          | 👍 легко           | 👍 Redis уже есть | 👍/⚠️ lightweight cluster       |

---

# 🎯 Ключевые выводы для собеса

1. **Kafka** = мощный, надёжный, хранит события, идеально для потоковой аналитики.
2. **RabbitMQ** = гибкий брокер задач и сообщений, удобен для workflow и pub-sub с routing.
3. **Redis Streams** = суперскоростной, лёгкий, идеально для ephemeral data, low-latency очередей.
4. **NATS Streaming** = лёгкий event bus, простая горизонтальная масштабируемость, идеально для микросервисов и IoT.

💬 **Фраза на собес:**

> «Выбор брокера зависит от нагрузки, требований к ретенции и консистентности: Kafka для event sourcing и аналитики, RabbitMQ для workflow и pub-sub, Redis Streams для сверхскоростных очередей, NATS Streaming для лёгкого low-latency event bus».
