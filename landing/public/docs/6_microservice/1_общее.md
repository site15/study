# 🧩 Основные паттерны микросервисов

Микросервисы — это не только «много маленьких сервисов», а архитектура, где **каждый сервис автономен**, общается через API/сообщения и решает свои бизнес-задачи.

Паттерны делятся на несколько категорий: **интеграция, коммуникация, хранение, устойчивость, инфраструктура**.

---

## 1️⃣ Паттерны интеграции сервисов

### **API Gateway**

* Сервис-агрегатор: единая точка входа для клиентов.
* Абстрагирует внутренние микросервисы.
* Можно добавлять auth, rate-limiting, кэширование.

**Пример:**

```text
Client → API Gateway → AuthService / OrderService / ProductService
```

### **Aggregator / Backend for Frontend (BFF)**

* Один endpoint собирает данные с нескольких сервисов.
* Часто для фронтенда: мобильного и вебного — разные BFF.

**Пример:**

* Web BFF → агрегирует Orders + Users
* Mobile BFF → агрегирует только основные поля

---

## 2️⃣ Паттерны коммуникации

### **Synchronous / REST**

* HTTP / gRPC вызовы между сервисами.
* Быстро, понятно, но **жёсткая связность**.
* Риск cascading failure.

### **Asynchronous / Event-driven**

* Через брокеры сообщений: RabbitMQ, Kafka, NATS.
* Сервисы подписываются на события.
* Повышает устойчивость и decoupling.

**Пример:**

```text
OrderService → emits event 'order_created' → InventoryService, EmailService consume
```

---

## 3️⃣ Паттерны хранения данных

### **Database per service**

* Каждый сервис владеет своей БД.
* Нет shared schema.
* Инварианты контролирует сам сервис.

**Плюсы:** изоляция, независимость
**Минусы:** сложные join-ы, eventual consistency

### **Shared database (редко)**

* Когда нужно быстро прототипировать
* Часто мешает масштабированию

---

## 4️⃣ Паттерны согласованности данных

### **Saga Pattern**

* Асинхронные транзакции через цепочку шагов + компенсации.
* Можно с событиями (Event Choreography) или через Orchestrator.

**Пример (онлайн-заказ):**

1. OrderService создаёт заказ
2. InventoryService резервирует товары
3. PaymentService списывает деньги
4. Если Payment fail → InventoryService отменяет резерв

---

### **Event Sourcing**

* Храним события, а не текущее состояние.
* State строится через replay событий.

**Пример:**

* `OrderCreated → ItemReserved → PaymentCharged → OrderCompleted`

---

### **CQRS (Command Query Responsibility Segregation)**

* Разделяем **команды** (write) и **запросы** (read)
* Read-DB может быть оптимизированной (ElasticSearch, Redis)
* Write-DB — source of truth

**Пример:**

```text
Write: OrderService → PostgreSQL
Read: OrderQueryService → Elasticsearch
```

---

## 5️⃣ Паттерны устойчивости

### **Circuit Breaker**

* Разрыв цепочки при падении внешнего сервиса
* Позволяет не блокировать систему
* Netflix Hystrix / Resilience4j

### **Bulkhead**

* Изоляция ресурсов сервисов
* Ошибка в одном модуле не ломает другие

### **Retry / Timeout / Backoff**

* При ошибках повторяем запрос с экспоненциальной задержкой

---

## 6️⃣ Паттерны доставки сообщений

### **Publish-Subscribe**

* Event → подписчики
* Для асинхронных уведомлений и интеграций

### **Work Queue**

* Задачи → очередь → воркеры
* Служит для background jobs, обработка heavy tasks

### **Dead Letter Queue**

* Сообщения, которые не удалось обработать
* Для анализа и повторной попытки

---

## 7️⃣ Паттерны наблюдаемости (Observability)

* **Log aggregation** → ELK / EFK
* **Metrics** → Prometheus, Grafana
* **Tracing** → OpenTelemetry / Jaeger
* Centralized monitoring → важно для продакшена

---

## 8️⃣ Паттерны деплоя и инфраструктуры

### **Blue-Green / Canary**

* Обновления без даунтайма
* Минимизируем риски при релизах

### **Sidecar**

* Дополнительный контейнер рядом с сервисом
* Для логирования, прокси, retries

### **Service Mesh**

* Istio, Linkerd
* Контроль трафика, retries, secure communication

---

## 9️⃣ Архитектурные рекомендации

1. **Разделяй сервисы по бизнес-границам** (Bounded Context)
2. **Используй event-driven там, где нужна decoupling**
3. **Saga + CQRS** для сложных бизнес-процессов
4. **API Gateway / BFF** для фронтенда
5. **Observability** обязательна, иначе «невидимые ошибки» в проде
6. **Изоляция базы и ресурсов** → deadlocks и backpressure решаются на уровне сервисов

---

# 🎯 Реальный пример микросервисной системы

**E-commerce:**

* **API Gateway** → принимает запросы от клиента
* **AuthService** → JWT / OAuth
* **OrderService** → создаёт заказы, владеет своей БД
* **InventoryService** → резервирует товар
* **PaymentService** → списывает деньги
* **EmailService / NotificationService** → подписаны на события order_created
* **Event Bus** → Kafka / RabbitMQ
* **Read side / Reporting** → CQRS: Elasticsearch + ClickHouse
* **Saga Orchestrator** → управляет order workflow