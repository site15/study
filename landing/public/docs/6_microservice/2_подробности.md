# 1️⃣ **API Gateway**

### Что это

* Центральная точка входа для всех клиентов (мобильные приложения, веб, внешние сервисы).
* Скрывает внутреннюю структуру микросервисов.

### Зачем нужен

* Агрегирует ответы от нескольких сервисов в один
* Добавляет кросс-сервисные функции: auth, rate-limiting, logging, кэширование

### Пример

```text
Client → API Gateway → AuthService / OrderService / ProductService
```

* Клиент делает один запрос, API Gateway сам собирает данные с нескольких сервисов

### Плюсы

* Простота для клиентов
* Безопасность и контроль доступа
* Централизованное кэширование и throttling

### Минусы

* Single point of failure (если не кластеризовать)
* Дополнительная задержка на маршрутизации

---

# 2️⃣ **Backend for Frontend (BFF)**

### Что это

* Специальный микросервис под конкретный клиент (веб, мобильный)
* Собирает данные из нескольких сервисов и отдаёт их в удобном формате

### Зачем нужен

* Мобильное приложение не тянет лишние поля из всех сервисов
* Web может использовать более сложные структуры

### Пример

* Web BFF → агрегирует Users + Orders + Products
* Mobile BFF → отдаёт только ключевые данные для UI

### Плюсы

* Клиенты получают только то, что нужно
* Можно оптимизировать под разные типы устройств

### Минусы

* Дополнительный слой
* Нужно поддерживать несколько BFF

---

# 3️⃣ **Synchronous / REST communication**

### Что это

* Сервисы общаются напрямую через HTTP/gRPC

### Пример

```text
OrderService → HTTP POST → PaymentService
```

### Плюсы

* Простая реализация
* Быстрое понимание, легко дебажить

### Минусы

* Tight coupling (жёсткая связность)
* Риск cascading failure, если один сервис упал

---

# 4️⃣ **Asynchronous / Event-driven**

### Что это

* Сервисы общаются через **события**
* Брокеры: Kafka, RabbitMQ, NATS

### Пример

```text
OrderService → emits 'order_created' → InventoryService + EmailService consume
```

### Плюсы

* Loose coupling
* Высокая устойчивость
* Можно легко масштабировать

### Минусы

* Сложнее отлаживать
* Eventual consistency → нужно учитывать временные рассогласования

---

# 5️⃣ **Database per Service**

### Что это

* Каждый сервис владеет своей базой данных
* Другие сервисы обращаются только через API / events

### Пример

* OrderService → PostgreSQL
* InventoryService → MongoDB
* EmailService → Redis

### Плюсы

* Изоляция и независимость сервисов
* Каждый сервис выбирает подходящую технологию хранения

### Минусы

* Нет JOIN между сервисами → иногда сложно строить отчёты
* Eventual consistency вместо ACID

---

# 6️⃣ **Saga Pattern**

### Что это

* Управление **длинными бизнес-транзакциями** через цепочку шагов с компенсациями
* Каждый шаг — отдельная транзакция, ошибки компенсируются

### Пример (онлайн-заказ)

1. OrderService создаёт заказ
2. InventoryService резервирует товары
3. PaymentService списывает деньги
4. Если Payment fail → InventoryService отменяет резерв

### Плюсы

* Нет распределённых транзакций (2PC)
* Надёжность и отказоустойчивость

### Минусы

* Сложность реализации
* Нужно контролировать ошибки и компенсации

---

# 7️⃣ **Event Sourcing**

### Что это

* Сохраняем **события**, а не текущее состояние
* State можно восстановить через replay событий

### Пример

```
OrderCreated → ItemReserved → PaymentCharged → OrderCompleted
```

### Плюсы

* Audit trail: вся история действий
* Можно восстановить состояние сервиса

### Минусы

* Сложнее чтение текущего состояния
* Нужно хранить много событий

---

# 8️⃣ **CQRS (Command Query Responsibility Segregation)**

### Что это

* Разделяем **write (command)** и **read (query)**
* Read-DB может быть оптимизированной (ElasticSearch, Redis), Write-DB — source of truth

### Пример

```text
Write: OrderService → PostgreSQL
Read: OrderQueryService → Elasticsearch
```

### Плюсы

* Read оптимизирован под быстрые запросы
* Масштабирование отдельно read/write

### Минусы

* Eventual consistency между write/read
* Сложнее поддерживать

---

# 9️⃣ **Circuit Breaker**

### Что это

* Защита от cascading failure
* Если внешний сервис падает, Circuit Breaker разрывает цепь

### Пример

```text
PaymentService → CircuitBreaker → OrderService
```

### Плюсы

* Предотвращает блокировку всей системы
* Можно настроить fallback

### Минусы

* Нужно мониторить и настраивать thresholds

---

# 🔟 **Bulkhead**

### Что это

* Изоляция ресурсов разных сервисов
* Ошибка в одном сервисе не ломает остальные

### Пример

* Thread pool для EmailService, Thread pool для PaymentService

### Плюсы

* Устойчивость
* Нет cascade failure

### Минусы

* Сложнее планировать ресурсы

---

# 11️⃣ **Retry / Timeout / Backoff**

### Что это

* Повторная попытка запроса с экспоненциальной задержкой

### Пример

```text
OrderService → PaymentService
- retry 3 раза  
- backoff 500ms → 1000ms → 2000ms
```

### Плюсы

* Уменьшает влияние временных сбоев

### Минусы

* Нужно контролировать Idempotency, иначе дубли

---

# 12️⃣ **Dead Letter Queue (DLQ)**

### Что это

* Очередь для сообщений, которые **не удалось обработать**

### Пример

* RabbitMQ / Kafka: сообщение не ack’ed → DLQ

### Плюсы

* Можно анализировать и исправлять ошибки
* Система не блокируется

### Минусы

* Нужно мониторить DLQ

---

# 13️⃣ **Observability (Logs / Metrics / Tracing)**

### Что это

* Централизованное логирование
* Метрики + графики (Prometheus, Grafana)
* Distributed tracing (Jaeger, OpenTelemetry)

### Зачем

* Диагностика проблем
* SLA / SLO контроль
* Устойчивость к продакшен-сбоям

---

# 14️⃣ **Deployment patterns**

### **Blue-Green / Canary**

* Безопасные обновления
* Минимизируем downtime и риски

### **Sidecar**

* Дополнительный контейнер для logging, retries, service mesh

### **Service Mesh**

* Istio / Linkerd
* Управление трафиком, retries, security, observability
