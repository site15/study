# 🧩 Что нужно знать о DDD для продакшена и собеса

---

## 1️⃣ **Ubiquitous Language (общий язык)**

- Код и документация должны использовать **единый язык** с бизнес-экспертами.
- **Важно:** терминология из домена, а не из IT.

**Пример:**

- «Customer places Order» → `Order.place(customer)`
- Не писать `createRecord()` или `insertRow()`

---

## 2️⃣ **Bounded Context (границы контекста)**

- Чётко определяем **границы микросервисов или модулей**.
- Внутри контекста все термины однозначны.

**Важно знать:**

- **Context Map** — карта взаимодействий между контекстами
- **Shared Kernel** / **Anti-Corruption Layer** — как взаимодействовать с чужими моделями

---

## 3️⃣ **Aggregates (Агрегаты)**

- Объединяют сущности и value objects.
- **Root** — единственная точка входа для изменений.

**Что нужно помнить:**

- Aggregates должны быть **сбалансированы**:

  - Слишком маленький → много межсервисных вызовов
  - Слишком большой → блокировки и сложная согласованность

---

## 4️⃣ **Domain Events (События домена)**

- События — способ **развязать контексты** и синхронизировать состояния.
- **Event-driven architecture** тесно связана с DDD.

**Важно:**

- События должны быть **значимыми для бизнеса**, а не «technical events»
- Использовать как **источник данных для CQRS или интеграции сервисов**

---

## 5️⃣ **Domain Services**

- Сервисы, которые **не принадлежат конкретному агрегату**, но реализуют бизнес-логику.
- Чаще всего stateless.

**Пример:**

```ts
PaymentService.processPayment(order, paymentInfo);
```

---

## 6️⃣ **Repositories (Репозитории)**

- Абстракция доступа к агрегатам.
- **Важно:** сервисы работают с агрегатами через репозитории, не знают про базу напрямую.

**Пример:**

```ts
orderRepo.save(order);
const order = orderRepo.findById(orderId);
```

---

## 7️⃣ **Value Objects (Объекты-значения)**

- Immutable, сравниваются по значению, а не по идентификатору.
- Применяются для полей, которые логически «не имеют смысла без агрегата».

**Пример:**

```ts
Money { amount, currency }
Address { street, city, zip }
```

---

## 8️⃣ **Anti-Corruption Layer (ACL)**

- Защищает контекст от чужих моделей и систем.
- Пример: интеграция с legacy-системой → адаптер, который переводит чужую модель в свой домен.

---

## 9️⃣ **CQRS + Event Sourcing**

- DDD хорошо работает с **CQRS** и **Event Sourcing**:

  - CQRS → разделяем команды и запросы
  - Event Sourcing → все изменения через события

**Важно:**

- Нужно понимать, как агрегаты, события и проекции (read model) связаны.

---

## 🔟 **Consistency & Transaction Management**

- DDD требует осознанного подхода к **согласованности**.
- **Saga pattern** для распределённых транзакций: агрегаты изменяются через цепочку событий.
- **Важно понимать eventual vs strong consistency**.

---

## 1️⃣1️⃣ **Strategic Design vs Tactical Design**

- **Tactical DDD** → Entities, Aggregates, Value Objects, Repositories, Services
- **Strategic DDD** → Bounded Context, Context Map, Integration Patterns

**Сеньор должен знать оба уровня**.

---

## 1️⃣2️⃣ **Integration & Messaging Patterns**

- Event-driven + Saga → связи между контекстами
- Использование брокеров сообщений (Kafka, RabbitMQ, NATS)
- Поддержка **asynchronous workflows**

---

## 1️⃣3️⃣ **Modeling Skills**

- Способность **выделять агрегаты, сущности, события** из бизнес-требований
- Общение с domain experts → правильная модель
- Способность **оптимизировать глубину агрегатов** для производительности

---

## 1️⃣4️⃣ **Anti-patterns, которые нужно избегать**

- **Anemic Domain Model** → когда Entities пустые, а бизнес-логика всюду в сервисах
- **Big Ball of Mud** → нет границ контекста
- **Overly granular aggregates** → слишком мелкие агрегаты, сложная синхронизация
- **Ignoring domain events** → tight coupling между сервисами

---

## 🔹 Короткий чеклист сеньора по DDD

1. Модель отражает **бизнес-правила**
2. Чётко выделены **Bounded Contexts**
3. Агрегаты имеют **root entity** и управляют согласованностью
4. Используются **Value Objects и Domain Events**
5. Сервисы, репозитории, интеграция через events или API
6. Event-driven подход для **связи контекстов**
7. Saga pattern для **распределённых транзакций**
8. Контролируем **Consistency, Backpressure, Deadlocks** при интеграции
