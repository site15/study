# 🧠 Что такое DDD (Domain-Driven Design)

**DDD** — это подход к проектированию ПО, где **центр внимания — бизнес-домен**, а не технология.

* Фокус на **смысле и правилах предметной области**
* Помогает строить **сложные бизнес-системы**
* Приводит к архитектуре, где код отражает **реальные процессы компании**

> Основной тезис: модель должна быть **понятной бизнес-экспертам** и отражать **правила предметной области**.

---

# 🔹 Основные понятия DDD

## 1️⃣ **Domain (Домен)**

* Предметная область, которую решает система
* Пример: e-commerce, банковские переводы, складской учёт

---

## 2️⃣ **Bounded Context (Ограниченный контекст)**

* Явная граница, в которой **модель имеет смысл**
* За границей контекста теряется однозначность понятий
* Часто соответствует микросервису

**Пример:**

* **Order Context** → OrderService, PaymentService
* **Inventory Context** → InventoryService
* **Customer Context** → CustomerService

---

## 3️⃣ **Entities (Сущности)**

* Объекты с **уникальной идентичностью**, которые живут долго
* Изменяемые, имеют состояния

**Пример:**

```text
Order { id, customerId, items[], status }
```

---

## 4️⃣ **Value Objects (Объекты-значения)**

* Объекты **без идентичности**, полностью определяются значением
* Не изменяются, immutable

**Пример:**

```text
Money { amount: 100, currency: 'USD' }
Address { street, city, zip }
```

---

## 5️⃣ **Aggregates (Агрегаты)**

* Группа сущностей и value objects, которые образуют **единицу согласованности**
* Один root entity → точки входа для изменений
* Ограничивает доступ извне через root

**Пример:**

```
Order (root) → OrderItems (entities) → Money (value object)
```

> Любые изменения OrderItems проходят через Order root

---

## 6️⃣ **Repositories (Репозитории)**

* Абстракция хранения и извлечения агрегатов
* Не открывает детали хранения (SQL, NoSQL, Kafka)

**Пример:**

```ts
interface OrderRepository {
  save(order: Order): Promise<void>;
  findById(id: string): Promise<Order | null>;
}
```

---

## 7️⃣ **Domain Services**

* Логика, которая **не относится к конкретной сущности**, но важна для домена
* Не содержит состояния

**Пример:**

```text
PaymentService.processPayment(order, paymentInfo)
```

---

## 8️⃣ **Application Services**

* Координирует операции между агрегатами, репозиториями и внешними системами
* Сам по себе **не содержит бизнес-логики**

**Пример:**

```ts
class OrderAppService {
  constructor(orderRepo, paymentService) {}
  
  async createOrder(customerId, items) {
    const order = new Order(customerId, items);
    await this.orderRepo.save(order);
    await this.paymentService.process(order);
  }
}
```

---

## 9️⃣ **Domain Events**

* События, которые отражают **важные изменения в домене**
* Используются для интеграции между микросервисами

**Пример:**

```text
OrderCreated { orderId, customerId, totalAmount }
```

* InventoryService подписан на событие → резервирует товар

---

# 🔹 Паттерны и принципы DDD

1. **Ubiquitous Language (Общий язык)**

   * Все: бизнес-эксперты + разработчики используют один словарь
   * Код отражает термины бизнеса

2. **Modeling**

   * Код должен **отражать реальные процессы**
   * Entities, Value Objects, Aggregates

3. **Bounded Context**

   * Разделение системы на микросервисы/контексты
   * Связь через Events / API

4. **Anti-Corruption Layer (ACL)**

   * Защищает ваш контекст от чужих моделей
   * Например, адаптер для интеграции с legacy-системой

---

# 🔹 Пример реальной архитектуры на DDD (E-commerce)

```
OrderContext:
- Order (aggregate root)
- OrderItems (entities)
- OrderRepository
- OrderCreated (domain event)

InventoryContext:
- Inventory (aggregate root)
- InventoryRepository
- InventoryReserved (domain event)

PaymentContext:
- PaymentService (domain service)
- PaymentProcessed (domain event)
```

* OrderContext создаёт заказ → публикует `OrderCreated`
* InventoryContext подписан → резервирует товары → публикует `InventoryReserved`
* PaymentContext подписан → списывает деньги

---

# 🔹 Преимущества DDD

* Код отражает бизнес
* Чёткие границы сервисов → легче масштабировать
* Domain Events → decoupling микросервисов
* Aggregates → управляемая согласованность данных

---

# 🔹 Минусы / сложности

* Сложнее для маленьких проектов
* Требует сильного взаимодействия с бизнес-экспертами
* Нужно контролировать **глубину агрегации**, иначе «мегасервисы»

---

# 🎯 Ключевая фраза на собес

> DDD — это не только Entities и Value Objects, а **целостная стратегия**, где код, события и границы сервисов отражают бизнес-домен и упрощают масштабирование сложных систем.

> Bounded Context + Aggregates + Domain Events → основа микросервисной архитектуры с устойчивой согласованностью.