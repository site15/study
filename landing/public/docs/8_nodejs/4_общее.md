# 🧩 Node.js — типовое для сеньоров

---

## 1️⃣ **Event Loop & Асинхронность**

* **Что важно:** понимать порядок выполнения microtasks (Promises, process.nextTick) и macrotasks (setTimeout, setImmediate), влияние блокирующего кода на Event Loop.
* **Пример:**

```js
console.log('sync');
setTimeout(() => console.log('timeout'), 0);
Promise.resolve().then(() => console.log('promise'));
// Вывод: sync → promise → timeout
```

* **Продакшен-фокус:** избегать блокировки Event Loop, offload CPU-heavy операции в Worker Threads.

---

## 2️⃣ **Callbacks, Promises, Async/Await**

* Понимать разницу, порядок выполнения, обработку ошибок.
* **Пример:**

```ts
async function fetchData() {
  try {
    const user = await getUser();
    console.log(user);
  } catch(err) {
    console.error(err);
  }
}
```

* **Продакшен:** всегда оборачивать await в try/catch, особенно для внешних API.

---

## 3️⃣ **Worker Threads**

* Для CPU-heavy задач: вычисления, шифрование, сжатие.
* **Пример:**

```ts
const { Worker } = require('worker_threads');
new Worker('./heavyTask.js');
```

* **Продакшен:** Node.js остаётся responsive, обрабатывает миллионы запросов.

---

## 4️⃣ **Streams**

* Потоковое чтение/запись файлов или сетевых данных.
* **Пример:**

```ts
const fs = require('fs');
fs.createReadStream('bigfile.txt').pipe(process.stdout);
```

* **Продакшен:** memory-efficient, лучше чем readFile для больших файлов.

---

## 5️⃣ **Cluster / PM2**

* Масштабирование Node.js на несколько CPU (Cluster) или управляемый процесс (PM2).
* **Продакшен:** горизонтальное масштабирование, zero-downtime reloads.

---

## 6️⃣ **Error Handling**

* `uncaughtException` → аварийный shutdown, `unhandledRejection` → логирование.
* **Продакшен:** глобальные обработчики + корректный retry/promise handling.

---

## 7️⃣ **Performance / Monitoring**

* Метрики: Event Loop lag, heap usage, GC pauses
* Инструменты: `clinic.js`, `node --inspect`, Prometheus metrics
* **Продакшен:** мониторить latency, avoid memory leaks.

---

## 8️⃣ **HTTP & API Patterns**

* Express / Fastify / NestJS
* Middleware, error handling, rate limiting, logging
* **Продакшен:** безопасные REST/gRPC, CORS, throttling.

---

## 9️⃣ **Security Best Practices**

* Input validation, helmet, rate limiting
* JWT / OAuth / session management
* Avoid eval, prototype pollution, SSRF/XSS

---

## 🔟 **Event-driven Architecture**

* RabbitMQ / Kafka / NATS / Redis Streams
* Pub/Sub, Worker queues, backpressure, DLX
* **Продакшен:** асинхронные workflow, decoupling, saga patterns

---

## 1️⃣1️⃣ **Memory & Garbage Collection**

* Node.js имеет **single-threaded heap**, GC может блокировать Event Loop
* **Продакшен:** следить за heap snapshots, avoid memory leaks

---

## 1️⃣2️⃣ **Testing & CI/CD**

* Unit tests, integration tests, end-to-end
* Tools: Jest, Mocha, Supertest, nock
* **Продакшен:** CI/CD pipelines с линтингом, coverage и e2e тестами

---

## 1️⃣3️⃣ **SOLID & Clean Architecture**

* Single Responsibility, Dependency Inversion, etc.
* **Продакшен:** код структурирован по сервисам, репозиториям, domain events

---

## 1️⃣4️⃣ **TypeScript**

* Типизация → меньше runtime ошибок, автодополнение
* **Продакшен:** DTOs, interfaces, strict mode, generics

---

## 1️⃣5️⃣ **Database Integration**

* Async queries, connection pools
* Postgres (node-postgres, Prisma), MongoDB, Redis
* **Продакшен:** pool management, transaction handling, proper error handling

---

## 1️⃣6️⃣ **Logging & Observability**

* Winston, Pino, Bunyan
* Correlation IDs для request tracing
* **Продакшен:** структурированные логи, трассировка запросов, alerts

---

## 1️⃣7️⃣ **Concurrency & Throttling**

* Promise.all, Promise.allSettled, p-limit
* Rate-limiting для внешних сервисов
* **Продакшен:** избегаем overload Event Loop и downstream сервисов

---

## 1️⃣8️⃣ **Deployment Patterns**

* Docker, Kubernetes, CI/CD pipelines
* Zero-downtime deployments, health checks, readiness probes

---

### 🎯 Короткий чеклист для собеса

* Event Loop / Async / Worker Threads
* Promises / Async/Await / error handling
* Streams / backpressure
* Clustering / PM2
* Memory & GC monitoring
* Event-driven architecture (Kafka/RabbitMQ/NATS)
* SOLID, Clean Architecture
* TypeScript, logging, testing, deployment patterns