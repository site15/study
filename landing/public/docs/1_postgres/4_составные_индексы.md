# 🧩 Что такое составной индекс

**Составной индекс** — это индекс **по нескольким колонкам**, где **порядок колонок критически важен**.

```sql
CREATE INDEX ON "Orders" ("tenantId", "createdAt", "status");
```

Это **НЕ три индекса**, а **один индекс с упорядоченными ключами**.

---

## 🧠 Главный принцип (must know)

> **Работает правило левого префикса**

Индекс используется **только начиная с первой колонки**.

---

## ✔️ Какие запросы используют индекс

Для индекса:

```sql
("tenantId", "createdAt", "status")
```

### ✅ Используется

```sql
WHERE tenantId = ?
```

```sql
WHERE tenantId = ? AND createdAt >= ?
```

```sql
WHERE tenantId = ? AND createdAt >= ? AND status = ?
```

### ❌ НЕ используется (или частично)

```sql
WHERE createdAt >= ?
```

```sql
WHERE status = ?
```

```sql
WHERE createdAt >= ? AND status = ?
```

---

## 🔥 Очень важный нюанс: `=` и диапазоны

Как только в индексе встречается **диапазон** (`> < BETWEEN`),
**всё, что справа — уже не участвует в поиске**, только в фильтрации.

### Пример

```sql
INDEX ("tenantId", "createdAt", "status")
```

```sql
WHERE tenantId = ?        -- индекс
  AND createdAt >= ?     -- индекс
  AND status = ?         -- ❌ уже не используется для поиска
```

---

## 📐 Как правильно выбирать порядок колонок

### Базовое правило

1. **Сначала equality (`=`)**
2. Потом диапазоны
3. Потом поля для сортировки

### Практический пример

Запрос:

```sql
SELECT *
FROM "Orders"
WHERE tenantId = ?
  AND status = 'paid'
  AND createdAt >= ?
ORDER BY createdAt DESC;
```

### Правильный индекс

```sql
CREATE INDEX
ON "Orders" ("tenantId", "status", "createdAt");
```

---

## 📦 Covering index (Index Only Scan)

Если индекс содержит **все поля запроса**, PostgreSQL может **не читать таблицу вообще**.

```sql
CREATE INDEX
ON "Orders" ("tenantId", "status", "createdAt")
INCLUDE ("amount");
```

```sql
SELECT createdAt, amount
FROM "Orders"
WHERE tenantId = ?
  AND status = 'paid';
```

👉 Возможен **Index Only Scan**.

---

## ⚠️ Частые ошибки

### ❌ Индекс «на всякий случай»

```sql
("tenantId", "createdAt", "status", "type", "currency")
```

— огромный, дорогой, редко используется полностью.

---

### ❌ Неправильный порядок

```sql
("createdAt", "tenantId")
```

при основном фильтре по `tenantId`.

---

### ❌ Дублирование индексов

```sql
INDEX (tenantId)
INDEX (tenantId, createdAt)
```

— первый часто **лишний**.

---

## 🎤 Как красиво сказать на собесе

> «Составной индекс работает по правилу левого префикса, поэтому порядок колонок я выбираю исходя из equality-условий, диапазонов и сортировки конкретных запросов.»

---

## 🧠 Короткая шпаргалка

| Ситуация         | Решение         |
| ---------------- | --------------- |
| много `=`        | ставь первыми   |
| есть `BETWEEN`   | после `=`       |
| нужен `ORDER BY` | в конец индекса |
| читаем часто     | `INCLUDE`       |
| пишем часто      | минимум колонок |
