# 🧠 Что такое Eventual Consistency (очень коротко)

> **Данные могут быть временно неконсистентны**,
> но **в итоге** придут в согласованное состояние.

Ключевое слово — **временно**.
Сколько это «временно» — **никто не гарантирует**.

---

# ⚖️ Strong vs Eventual (интуитивно)

| ACID (Postgres)    | Eventual (NoSQL)               |
| ------------------ | ------------------------------ |
| COMMIT → все видят | COMMIT → *когда-нибудь увидят* |
| строгая логика     | высокая доступность            |
| блокировки         | асинхронность                  |

---

# 🧨 Реальные баги из жизни

## 🐛 Баг №1: «Деньги списались, но баланс не обновился»

### Архитектура

* Redis / DynamoDB
* запись в один узел
* чтение из другого

### Сценарий

```
T1: списали 100₽ → запись ушла в реплику A
T2: читаем баланс → читаем из реплики B
```

👉 пользователь видит **старый баланс**

### Почему

* репликация асинхронная
* read-after-write не гарантирован

### Как фиксили

* read from leader
* write-through cache
* versioning / CAS

---

## 🐛 Баг №2: «Заказ есть, товаров нет»

### MongoDB (денормализация)

```json
Order {
  orderId: 1,
  items: [{ productId: 10, qty: 2 }]
}
```

```json
Product {
  productId: 10,
  stock: 1
}
```

### Сценарий

* Order записался
* Product не обновился (timeout / retry)
* данные **разъехались навсегда**

👉 заказ есть, товара нет

### Почему

* нет distributed transaction
* write split на 2 сущности

### Как фиксили

* single document update
* saga
* outbox pattern

---

## 🐛 Баг №3: «Два пользователя купили последний товар»

### Cassandra / DynamoDB

```text
stock = 1
```

```
T1: read stock = 1
T2: read stock = 1
T1: write stock = 0
T2: write stock = 0
```

👉 **double sell**

### Почему

* нет блокировок
* eventual consistency
* last write wins

### Фикс

* conditional update (CAS)
* LWT (Lightweight Transaction)
* вынести в SQL

---

## 🐛 Баг №4: «Поиск не находит только что созданный объект»

### Elasticsearch

```
POST /orders
→ 200 OK
```

```
GET /search?orderId=123
→ пусто
```

Через 1–2 секунды — появляется.

### Почему

* индексирование асинхронное
* refresh interval

### Как живут с этим

* не искать сразу
* fallback в SQL
* read-your-writes через primary storage

---

## 🐛 Баг №5: «Счётчик лайков прыгает»

### Redis / DynamoDB counter

```
likes = 10
```

* пользователь лайкнул
* видит 9
* обновил → 11

### Почему

* разные ноды
* race
* replication lag

### Фикс

* atomic increment
* single writer
* CRDT (если умеешь — плюс к карме)

---

# 🧠 Типовые классы багов при Eventual Consistency

| Тип бага         | Симптом                 |
| ---------------- | ----------------------- |
| Read-after-write | не видно свои изменения |
| Lost update      | данные перезаписались   |
| Double write     | дубли                   |
| Write skew       | логика сломалась        |
| Stale read       | старые данные           |

---

# 🛠 Паттерны борьбы (обязательно знать)

## 1️⃣ Read Your Writes

* читать из того же шарда / лидера
* sticky sessions

---

## 2️⃣ Versioning

```json
{ "value": 10, "version": 5 }
```

👉 optimistic locking

---

## 3️⃣ Saga

* цепочка шагов
* компенсации

```text
createOrder
↓
reserveStock
↓
chargePayment
↓
confirm
```

---

## 4️⃣ Outbox Pattern

* событие пишется **в одной транзакции**
* дальше асинхронно

---

## 5️⃣ Idempotency

```text
requestId = UUID
```

👉 повторный запрос не ломает систему

---

# 🎤 Золотые фразы для собеса

> Eventual consistency — это не баг, а контракт системы, но бизнес должен быть к этому готов.

> Если операция финансовая или уникальная — я не использую eventual consistency.

> Большинство багов в NoSQL — это не проблемы БД, а проблемы ожиданий разработчиков.

---

# 🧠 Как понять, что NoSQL не подходит

❌ деньги
❌ уникальность
❌ строгие инварианты
❌ сложные бизнес-правила

👉 **PostgreSQL + транзакции**

---

# 🎯 Короткая шпаргалка

| Нужна гарантия   | Выбор |
| ---------------- | ----- |
| read-after-write | SQL   |
| атомарность      | SQL   |
| масштаб          | NoSQL |
| скорость         | NoSQL |
| бизнес-логика    | SQL   |
