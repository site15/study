# 🧠 Что такое NoSQL (без сказок)

**NoSQL** — это не «без SQL», а:

> базы данных, которые **осознанно нарушают реляционную модель**
> ради **масштабирования, скорости или гибкости схемы**

Главное отличие:

* ❌ нет JOIN
* ❌ нет строгой схемы
* ❌ часто нет ACID целиком
* ✅ горизонтальное масштабирование
* ✅ высокая пропускная способность

---

# 🧩 Основные типы NoSQL

## 1️⃣ Key–Value

**Redis, DynamoDB**

### Как хранят

```
key → value
```

### Реальный кейс

**Сессии пользователей**

```text
session:abc123 → { userId: 42, expiresAt: ... }
```

### Почему не PostgreSQL

* миллионы операций в секунду
* TTL
* данные не критичны

### Пример (Redis)

```redis
SET session:abc123 "{userId:42}"
EXPIRE session:abc123 3600
```

👉 если Redis упадёт — пользователь просто перелогинится

---

## 2️⃣ Document Store

**MongoDB, CouchDB**

### Как хранят

JSON-документы:

```json
{
  "id": 1,
  "userId": 42,
  "items": [
    { "productId": 10, "qty": 2 }
  ],
  "status": "paid"
}
```

### Реальный кейс

**Каталоги, профили, формы, CMS**

Почему удобно:

* структура часто меняется
* вложенные данные
* нет JOIN → всё в одном документе

### Пример (MongoDB)

```js
db.orders.find({
  userId: 42,
  status: "paid"
})
```

❗ Но:

* дублирование данных
* сложно поддерживать консистентность
* транзакции ограничены

---

## 3️⃣ Wide Column Store

**Cassandra, ScyllaDB**

### Как хранят

```
partition_key → rows → columns
```

### Реальный кейс

**Логи, метрики, события**

```text
(userId, date) → [timestamp → value]
```

### Пример

```sql
SELECT *
FROM user_events
WHERE userId = 42
  AND date = '2026-01-01';
```

👉 запросы **только по ключу**, никаких JOIN

### Почему используют

* миллиарды записей
* линейное масштабирование
* write-heavy нагрузки

---

## 4️⃣ Graph DB

**Neo4j**

### Как хранят

```
(Users)-[:FRIEND]->(Users)
```

### Реальный кейс

* соцсети
* рекомендации
* антифрод
* связи и зависимости

### Пример

```cypher
MATCH (u:User)-[:FRIEND]->(f:User)
WHERE u.id = 42
RETURN f;
```

👉 в SQL это был бы ад из JOIN’ов

---

# ⚖️ ACID vs BASE (ключевая тема)

## ACID (PostgreSQL)

* строго
* надёжно
* медленнее масштабируется

## BASE (NoSQL)

* **Basically Available**
* **Soft state**
* **Eventually consistent**

👉 данные могут быть **временно неконсистентны**

---

# 🔥 Реальные архитектурные примеры

## 🏗 Пример 1: типичный highload-проект

| Задача                 | БД            |
| ---------------------- | ------------- |
| основная бизнес-логика | PostgreSQL    |
| сессии                 | Redis         |
| кэш                    | Redis         |
| логи                   | Cassandra     |
| аналитика              | ClickHouse    |
| поиск                  | Elasticsearch |

👉 **NoSQL не заменяет SQL**, он его дополняет

---

## 🧪 Пример 2: очередь задач

```sql
-- PostgreSQL (плохо)
SELECT * FROM jobs WHERE status='new';
```

```sql
-- Redis (хорошо)
LPUSH jobs_queue job_id
```

Почему Redis:

* атомарные операции
* скорость
* простота

---

## 💣 Типичные ошибки (ловушки на собесе)

### ❌ «MongoDB вместо PostgreSQL»

→ потом боль с:

* транзакциями
* отчётами
* консистентностью

---

### ❌ JOIN в голове

```text
User
Order
OrderItems
```

в NoSQL → **денормализация**

```json
Order {
  userName,
  items[],
  total
}
```

---

## 🧠 Когда NoSQL — правильный выбор

✔ огромный поток данных
✔ простые запросы
✔ eventual consistency допустима
✔ горизонтальное масштабирование

## 🚫 Когда нельзя

❌ финансы
❌ строгая целостность
❌ сложная аналитика
❌ отчёты и JOIN’ы

---

# 🎤 Как красиво сказать на собесе

> NoSQL — это не альтернатива PostgreSQL, а инструмент под конкретные сценарии: кэш, очереди, события, графы и высоконагруженные записи.

> В реальных системах почти всегда используется polyglot persistence.

---

# 🧠 Короткая шпаргалка

| Тип        | Когда                |
| ---------- | -------------------- |
| Redis      | кэш, сессии, очереди |
| MongoDB    | документы, формы     |
| Cassandra  | логи, метрики        |
| Neo4j      | связи                |
| PostgreSQL | бизнес-данные        |
