# 🧩 Requirements & Scale в System Design

Когда тебе дают задачу типа **“Спроектируй систему чата на миллион пользователей”**, первое, что нужно делать — это **собрать и уточнить требования**, а потом оценить **масштабируемость**.

---

## 1️⃣ **Функциональные требования (Functional Requirements)**

**Что выяснять:**

* **Что делает система?**

  * Пользователи могут отправлять/получать сообщения
  * Поиск по сообщениям
  * Уведомления (push/email)
* **Граничные условия**

  * Максимальная длина сообщения
  * Ограничение на вложения
  * Количество одновременных подключений

**Пример уточняющих вопросов на собесе:**

* «Нужно ли хранить историю сообщений навсегда?»
* «Должна ли система поддерживать групповые чаты?»
* «Какие типы уведомлений нужны?»

---

## 2️⃣ **Нефункциональные требования (Non-Functional Requirements)**

**Что выяснять:**

* **Throughput / QPS / TPS**

  * Сколько запросов в секунду? Сколько сообщений в секунду?
* **Latency**

  * Время отклика для чтения/отправки сообщения
* **Availability / SLA**

  * Сколько % времени система должна быть доступна?
* **Storage / Retention**

  * Сколько данных хранится, как долго?
* **Scalability**

  * Горизонтальная (добавление серверов) vs вертикальная масштабируемость

**Пример:**

* «Система должна обрабатывать 50 000 сообщений в секунду с задержкой <100ms»
* «История сообщений хранится 1 год»

---

## 3️⃣ **Оценка нагрузки (Back-of-the-Envelope Calculation)**

* **Кол-во пользователей**: 1 млн
* **Активные пользователи в секунду**: 10% → 100 000
* **Среднее количество сообщений / с**: 1 → 100 000 сообщений / с
* **Размер одного сообщения**: 1 KB → ~100 MB/s поток данных

**Почему важно:**

* Позволяет понять, какие компоненты **нужны для масштабирования**: очередь сообщений, кэш, шардирование БД, worker-пулы.

---

## 4️⃣ **Масштабируемость (Scalability)**

### 🔹 Горизонтальная (Horizontal Scaling)

* Добавление серверов/нод
* Применимо для:

  * NestJS сервисов (через Kubernetes / PM2)
  * Брокеров сообщений (RabbitMQ, Kafka)
  * API Gateway / Load Balancer

**Пример:**

* Подключаем cluster Node.js / Kubernetes pods для chat-service
* Нагрузку распределяем через Nginx / HAProxy

### 🔹 Вертикальная (Vertical Scaling)

* Добавление ресурсов одному серверу (CPU/RAM)
* Применимо временно, но **ограничено ресурсами машины**

---

## 5️⃣ **Базы данных и шардирование**

* Для больших нагрузок **1 БД не справится**
* Шардирование по userId или tenantId → распределение нагрузки
* Репликация → чтение с реплик, запись на лидера

**Пример:**

* PostgreSQL для аккаунтов / relationships
* MongoDB для истории сообщений (документы)

---

## 6️⃣ **Кэширование и очередь сообщений**

* Redis → кэш последних сообщений, быстрый доступ
* RabbitMQ/Kafka → асинхронная обработка уведомлений, событий
* **Почему важно для масштабируемости:**

  * Разгружает БД
  * Устраняет блокировку Event Loop на Node.js

---

## 7️⃣ **Типовые вопросы на собесе по Requirements & Scale**

1. Как определить, сколько пользователей будет одновременно?
2. Как оценить нагрузку на сервис и базу?
3. Какие bottlenecks могут появиться при росте?
4. Как масштабировать NestJS сервис под миллионы сообщений?
5. Как хранить историю сообщений эффективно?

---

### 🔹 Ключевая мысль

> **Первый шаг в System Design — понять требования и оценить масштабируемость**.
> Без этого невозможно правильно спроектировать:
>
> * архитектуру микросервисов
> * выбор баз данных
> * очереди сообщений
> * кэширование
> * балансировку нагрузки
