# 🧩 Сложность алгоритмов на примере NestJS

---

## 1️⃣ **O(1) — константная сложность**

**Что это:** выполнение не зависит от размера данных

**Пример:** получение пользователя по id через репозиторий

```ts
@Get(':id')
async getUser(@Param('id') id: string) {
  return this.userRepository.findOneBy({ id }); // индекс по PK → O(1)
}
```

* **Почему O(1):** база использует индекс → один шаг
* **Продакшен:** быстрый доступ к данным, не важно, сколько пользователей в БД

---

## 2️⃣ **O(n) — линейная сложность**

**Что это:** количество операций растёт линейно с количеством элементов

**Пример:** поиск пользователя по email без индекса

```ts
@Get('search')
async searchUser(@Query('email') email: string) {
  const users = await this.userRepository.find();
  return users.find(u => u.email === email); // перебор массива
}
```

* **Почему O(n):** перебираем каждый элемент
* **Продакшен:** плохо на больших таблицах → используем индекс

---

## 3️⃣ **O(log n) — логарифмическая сложность**

**Что это:** на каждом шаге уменьшаем объём данных вдвое

**Пример:** бинарный поиск отсортированного массива users

```ts
function binarySearch(users: User[], email: string) {
  let left = 0, right = users.length - 1;
  while (left <= right) {
    const mid = Math.floor((left + right) / 2);
    if (users[mid].email === email) return users[mid];
    if (users[mid].email < email) left = mid + 1;
    else right = mid - 1;
  }
  return null;
}
```

* **Почему O(log n):** каждый шаг делим массив пополам
* **Продакшен:** редко нужен вручную, чаще база/индекс делает это за нас

---

## 4️⃣ **O(n log n) — типичная сортировка**

**Пример:** сортировка списка заказов

```ts
@Get('orders')
async getSortedOrders() {
  const orders = await this.orderRepository.find();
  return orders.sort((a, b) => a.createdAt.getTime() - b.createdAt.getTime());
}
```

* **Почему O(n log n):** mergeSort / quickSort в JS
* **Продакшен:** для больших списков лучше сортировать на уровне БД

---

## 5️⃣ **O(n²) — квадратичная сложность**

**Пример:** поиск дубликатов в списке пользователей

```ts
@Get('duplicates')
async findDuplicates() {
  const users = await this.userRepository.find();
  const duplicates = [];
  for (let i = 0; i < users.length; i++) {
    for (let j = i + 1; j < users.length; j++) {
      if (users[i].email === users[j].email) duplicates.push(users[i]);
    }
  }
  return duplicates;
}
```

* **Почему O(n²):** вложенный цикл
* **Продакшен:** плохо → для больших массивов используем Set/Map → O(n)

---

## 6️⃣ **O(n!) — факториальная сложность**

**Пример:** перебор всех комбинаций товаров в корзине

```ts
function permute(items: any[]): any[][] {
  if (items.length === 0) return [[]];
  return items.flatMap((item, i) =>
    permute(items.slice(0, i).concat(items.slice(i + 1))).map(p => [item, ...p])
  );
}
```

* **Почему O(n!):** количество перестановок растёт факториально
* **Продакшен:** только для небольших наборов или с ограничениями

---

# 🔹 Визуальная схема "на пальцах"

```
Количество данных (N) →
O(1)       - константа
O(log n)   - бинарный поиск / индекс в БД
O(n)       - перебор массива / full table scan
O(n log n) - сортировка
O(n²)      - вложенные циклы / поиск дубликатов
O(n!)      - все перестановки / комбинаторика
```

* **Практический совет:**

  * Всегда проверяйте **индексы БД** → часто меняет O(n) на O(1) или O(log n)
  * Используйте **Map/Set для поиска и фильтрации** → O(n) вместо O(n²)
  * Для больших данных — **обрабатывайте на стороне БД или потоками (stream)**