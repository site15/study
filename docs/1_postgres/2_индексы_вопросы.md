# 🧠 БАЗОВЫЕ ВОПРОСЫ (разогрев)

### **1. Какие типы индексов есть в PostgreSQL?**
**Ожидают услышать:**
> B-tree, Hash, GIN, GiST, SP-GiST, BRIN.

**Сеньор-добавка:**
> В 90% случаев используется B-tree, остальные — под специфические типы данных и операторы.

---

### **2. Какой индекс используется по умолчанию и почему?**
**Ответ:**
> B-tree, потому что он поддерживает equality, диапазоны и сортировки, а также работает с большинством операторов.

---

### **3. Какие операторы поддерживает B-tree?**
**Ответ:**
> `=`, `<`, `<=`, `>`, `>=`, `BETWEEN`, `IS NULL`, `ORDER BY`.

---

# ⚠️ КОВАРНЫЕ ВОПРОСЫ (любят на мид+ / сеньор)

### **4. Почему запрос не использует индекс?**
**Хороший ответ:**
- низкая селективность
- `WHERE` с функцией (`LOWER(col)`)
- несоответствие типов
- маленькая таблица
- плохая статистика
- `enable_seqscan = on` выгоднее

**Сеньор-фраза:**
> Планировщик выбирает самый дешёвый план, а не “план с индексом”.

---

### **5. Используется ли индекс при `LIKE '%text%'`?**
**Ответ:**
> Нет, обычный B-tree не используется.  
> Возможны варианты:
- `LIKE 'text%'` → может использоваться
- `GIN/GiST + pg_trgm` → для `%text%`

---

### **6. Что быстрее: Hash или B-tree?**
**Ответ:**
> Hash может быть чуть быстрее для `=`, но B-tree универсальнее и почти всегда предпочтительнее.

**Красный флаг:**  
если кандидат говорит *«Hash всегда быстрее»*.

---

### **7. Когда GIN — плохая идея?**
**Ответ:**
> При частых `INSERT/UPDATE`, потому что GIN дорог в поддержке и может сильно замедлять запись.

---

# 🧩 ПРАКТИЧЕСКИЕ КЕЙСЫ (самое вкусное)

### **8. Какой индекс выбрать для JSONB?**
**Ответ:**
> GIN, чаще всего `jsonb_ops` или `jsonb_path_ops` — зависит от запросов.

**Сеньор-добавка:**
> `jsonb_path_ops` компактнее и быстрее, но поддерживает меньше операторов.

---

### **9. Когда стоит использовать BRIN вместо B-tree?**
**Ответ:**
> Для очень больших таблиц, где данные коррелируют с физическим порядком — например, `createdAt`.

---

### **10. Что такое селективность индекса?**
**Ответ:**
> Это отношение уникальных значений к количеству строк.  
> Чем выше селективность — тем выгоднее индекс.

---

### **11. Может ли PostgreSQL использовать несколько индексов в одном запросе?**
**Ответ:**
> Да, через Bitmap Index Scan.

**Пример:**
```sql
WHERE status = 'active'
  AND createdAt > now() - interval '1 day'
```

---

### **12. В каком порядке важны колонки в составном индексе?**
**Ответ:**
> Критически важен порядок.  
> Используется **левый префикс**.

```sql
INDEX (tenantId, createdAt)
```

✔ `tenantId = ? AND createdAt > ?`  
❌ `createdAt > ?` — не использует индекс полностью

---

# 🔥 ВОПРОСЫ ДЛЯ СИЛЬНОГО СЕНЬОРА

### **13. Чем отличается Index Scan от Bitmap Index Scan?**
**Ответ:**
- Index Scan — точечный доступ
- Bitmap — когда подходит несколько индексов или много строк

---

### **14. Почему `COUNT(*)` может не использовать индекс?**
**Ответ:**
> Потому что PostgreSQL может выбрать Seq Scan, если считает его дешевле.

---

### **15. Что такое covering index?**
**Ответ:**
> Индекс, который содержит все поля запроса → возможен Index Only Scan.

---

### **16. Когда Index Only Scan невозможен?**
**Ответ:**
- данные не видны через visibility map
- частые UPDATE
- таблица недавно изменялась

---

### **17. Чем опасны лишние индексы?**
**Ответ:**
- замедляют INSERT/UPDATE/DELETE
- увеличивают размер базы
- усложняют планировщик

---

# 🎯 КОРОТКИЙ БЛОК «НА ДОБИВКУ»

### **18. Как проверить, используется ли индекс?**
```sql
EXPLAIN (ANALYZE, BUFFERS)
```

---

### **19. Что делать, если планировщик ошибается?**
**Ответ:**
- ANALYZE
- пересмотреть индекс
- переписать запрос
- крайне редко — `enable_seqscan = off` (и то временно)

---

### **20. Назови типичную ошибку проектирования индексов**
**Ответ:**
> Индекс “на всякий случай”, без реального запроса.

---

## 💬 Фраза, которая сильно повышает уровень на собесе
> «Индекс — это компромисс между скоростью чтения и ценой записи»