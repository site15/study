## **Что такое B-tree**

B-tree — это **сбалансированное дерево**, которое хранит данные в отсортированном виде.

* Каждая вершина дерева может содержать **несколько ключей** и ссылки на дочерние узлы.
* Все листья находятся на **одном уровне** → дерево всегда сбалансировано.
* Используется для **быстрого поиска, диапазонов и сортировок**.

---

## **Как это работает простыми словами**

Представь себе телефонную книгу:

1. **Внутренние узлы** — это буквы или диапазоны, которые помогают быстро ориентироваться.
2. **Листья** — это сами записи (имя → телефон).
3. **Ищем запись** — идем по дереву сверху вниз:

   * Сравниваем с ключами в узле
   * Переходим к нужной ветке
   * Дошли до листа → нашли запись

В среднем поиск работает за **O(log n)**, независимо от размера таблицы.

---

### **Пример на пальцах**

У тебя есть таблица `Users` с колонкой `id`:

```
[10, 20, 30, 40, 50, 60, 70, 80]
```

B-tree создаёт структуру:

```
       [40]
     /      \
 [20]        [60]
 /   \      /   \
[10] [30] [50] [70, 80]
```

* Ищем `30` → идём `40 → 20 → 30` → быстро нашли.
* Добавляем `25` → дерево перестраивается, остаётся сбалансированным.

---

### **Почему это важно для PostgreSQL**

* B-tree автоматически **поддерживает порядок** → можно делать `ORDER BY`.
* Подходит для **поиска по точному значению и диапазонам** → `=`, `<`, `>`, `BETWEEN`.
* Обеспечивает **уникальные индексы**, потому что дерево само сортирует ключи.

---

### 💡 Короткая мысль для собеса

> B-tree — это как телефонная книга для базы: всё отсортировано, балансируется и позволяет быстро найти любую запись, даже если база огромная.