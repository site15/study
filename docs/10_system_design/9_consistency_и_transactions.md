# 🧩 Consistency & Transactions в System Design

В распределённых системах **consistency (согласованность)** — это **надежность данных между сервисами и базами**, а **transactions (транзакции)** — способ **гарантированно обновлять несколько ресурсов атомарно**.

> В монолитах проще — ACID-транзакции базы данных.
> В микросервисах и распределённых системах — сложнее: несколько сервисов, разные БД, очереди, кеши.

---

## 1️⃣ Типы согласованности

| Тип                                          | Описание                                                       | Применение                         |
| -------------------------------------------- | -------------------------------------------------------------- | ---------------------------------- |
| **Strong Consistency (сильная)**             | Все клиенты видят одни и те же данные сразу после обновления   | Банковские переводы, платежи       |
| **Eventual Consistency (слабая / eventual)** | Данные могут быть временно рассогласованы, но в итоге сходятся | Кэш, соцсети, feed, аналитика      |
| **Read-your-writes**                         | Клиент видит свои последние изменения                          | UX для форм / профиля пользователя |

---

## 2️⃣ ACID vs BASE

**ACID (PostgreSQL, MySQL)**

- **Atomicity** → либо всё обновилось, либо ничего
- **Consistency** → данные всегда корректны
- **Isolation** → транзакции не мешают друг другу
- **Durability** → данные сохраняются при сбое

**BASE (NoSQL, Event-driven)**

- **Basically Available** → система доступна
- **Soft state** → состояние может быть временно рассогласовано
- **Eventually consistent** → согласованность придёт позже

> Сеньор должен понимать trade-offs: ACID → медленнее, BASE → масштабируется

---

## 3️⃣ Транзакции в микросервисах

### 🔹 Локальные транзакции

- Классическая транзакция внутри одного сервиса/БД
- NestJS + TypeORM / Prisma:

```ts
await this.dataSource.transaction(async (manager) => {
  await manager.save(user);
  await manager.save(order);
});
```

- **ACID соблюдается**, всё атомарно

---

### 🔹 Распределённые транзакции

- Несколько сервисов → разные базы
- **2PC (Two-Phase Commit)** → сложно, блокирует сервисы, редко используют

---

### 🔹 Saga Pattern

- Асинхронная альтернатива 2PC
- Каждая операция — локальная транзакция
- При ошибке — выполняется **компенсирующая операция**

**Пример: заказ в e-commerce**

1. OrderService создаёт заказ (локальная транзакция)
2. InventoryService резервирует товар (локальная транзакция)
3. PaymentService списывает деньги (локальная транзакция)
4. Если оплата не прошла → InventoryService отменяет резерв → OrderService отменяет заказ

**Реализация в NestJS:**

- Event-driven → через Kafka / RabbitMQ
- Каждый сервис публикует **domain event** → подписчики реагируют

---

## 4️⃣ Event-driven consistency

- **Eventual Consistency** → данные могут быть временно рассогласованы
- **Пример:** кэш в Redis может быть не синхронизирован сразу после обновления базы
- **Решение:** TTL, invalidation, read-through / write-through cache patterns

---

## 5️⃣ Practical tips для собеса

1. **Нужно понимать trade-offs:**

   - ACID → банковские операции, строгая целостность
   - Eventual → feed, кэш, аналитика

2. **Saga + events** → предпочтительный паттерн для микросервисов
3. **Compensation vs rollback** → rollback невозможен между сервисами, нужна компенсация
4. **Distributed tracing** → для отладки рассогласованности
5. **Idempotency** → операции повторно безопасны (payment retry, events replay)

---

## 6️⃣ Типичные вопросы на собесе

- «Как обеспечить согласованность заказов и платежей между сервисами?»
- «Почему 2PC не всегда подходит?»
- «Как реализовать Saga для микросервисов в Node.js?»
- «Как обрабатывать eventual consistency для кэша и поисковых индексов?»

---

## 7️⃣ Итог

- **Consistency & Transactions** — фундамент надежной распределённой системы
- **ACID** → внутри сервиса / БД
- **Saga / Event-driven / eventual consistency** → между сервисами
- Важно **понимать trade-offs и использовать подходящие паттерны в зависимости от требований SLA, latency и масштабируемости**
