# 🧩 API / Gateway / BFF в System Design

В системном дизайне **Gateway и BFF** — это **слой между клиентами и микросервисами**, который решает вопросы маршрутизации, безопасности, агрегации и адаптации данных под клиентские нужды.

---

## 1️⃣ **API Gateway**

**Что это:**

* Центральная точка входа для всех клиентских запросов.
* Абстрагирует внутренние микросервисы.
* Реализует маршрутизацию, авторизацию, кэширование, rate-limiting, logging.

**Зачем нужен:**

* Клиентам не нужно знать про множество микросервисов.
* Снижает связанность между фронтендом и внутренней системой.
* Централизованное управление безопасностью и лимитами.

**Продакшен-примеры:**

* NestJS + Fastify/Express + middleware для JWT, rate-limit, logging
* Nginx / Kong / Traefik как внешний API Gateway

**Сценарий:**

```
Client → API Gateway → AuthService / UserService / OrderService
```

**Что проверят на собесе:**

* Почему нужен gateway?
* Какие функции стоит вынести в Gateway, а какие оставить в микросервисах?

---

## 2️⃣ **Backend-for-Frontend (BFF)**

**Что это:**

* Сервис под конкретного клиента (например, web или mobile).
* Формирует ответы **оптимизированные под нужды клиента**, агрегируя данные из нескольких сервисов.
* Позволяет фронтенду получать **готовые структуры**, а не делать множество запросов.

**Зачем нужен:**

* Web может нуждаться в полном объекте, mobile — только в ключевых полях.
* Позволяет избежать **over-fetching / under-fetching**.

**Продакшен-пример:**

* Web BFF → агрегирует Users + Orders + Products
* Mobile BFF → отдаёт только краткие данные для отображения списка заказов

**Типовая схема:**

```
Web Client → Web BFF → UserService + OrderService + InventoryService
Mobile Client → Mobile BFF → OrderService (только ID и статус)
```

**Что проверят на собесе:**

* Когда стоит использовать BFF, а когда достаточно Gateway?
* Какие проблемы решает BFF? (latency, payload size, versioning API)

---

## 3️⃣ **Отличие API Gateway и BFF**

| Пункт         | API Gateway                               | BFF                                    |
| ------------- | ----------------------------------------- | -------------------------------------- |
| Основная цель | Маршрутизация, авторизация, rate-limiting | Адаптация данных под клиент, агрегация |
| Клиент        | Все клиенты                               | Один клиент / группа клиентов          |
| Применение    | Централизованная точка входа              | Индивидуальная логика фронтенда        |
| Пример        | Nginx / Kong / NestJS gateway             | NestJS сервис для Web / Mobile         |

---

## 4️⃣ **Продакшен-подход для NestJS**

* **Gateway:**

  * NestJS App Module с middleware: JWT auth, cors, rate-limit, logging
  * RouterModule / custom decorators для маршрутизации
* **BFF:**

  * NestJS microservice
  * AggregationService: запрашивает несколько микросервисов и формирует payload
  * Используем async/await, Promise.all для параллельных запросов

**Пример кода (BFF):**

```ts
@Injectable()
export class OrdersBffService {
  constructor(
    private ordersService: OrdersService,
    private inventoryService: InventoryService,
  ) {}

  async getUserOrders(userId: string) {
    const [orders, inventory] = await Promise.all([
      this.ordersService.findByUser(userId),
      this.inventoryService.getAvailableProducts(),
    ]);
    return orders.map(o => ({
      ...o,
      items: o.items.map(i => ({
        ...i,
        inStock: inventory.includes(i.productId),
      })),
    }));
  }
}
```

---

## 5️⃣ **Почему на собесе часто спрашивают**

1. Проверяют понимание **архитектурных паттернов**: Gateway и BFF
2. Оценивают **trade-offs**:

   * Gateway → проще, но payload может быть “сырой”
   * BFF → сложнее поддерживать несколько сервисов, но оптимизировано для клиента
3. Проверяют понимание **масштабирования и latency**: сколько вызовов к микросервисам делает BFF, как использовать кеш и асинхронные запросы

---

## 🔹 Ключевые takeaways

* **API Gateway:** общая точка входа, безопасность, маршрутизация
* **BFF:** сервис под конкретный клиент, адаптация и агрегация данных
* **NestJS продакшен:** async/await, Promise.all, микросервисы, middleware
* **Собес:** нужно уметь объяснять trade-offs, зачем нужен слой, как масштабировать и минимизировать latency
