# 🧩 High-Level Architecture Patterns для System Design

> High-Level Architecture — это **структура всей системы**, как модули и компоненты связаны между собой, какие технологии применяются и как обрабатываются потоки данных.

---

## 1️⃣ **Monolithic Architecture**

**Описание:**

- Все функции приложения в одном процессе/кодовой базе
- Общая база данных, один деплой

**Когда используют:**

- Маленькие проекты, MVP
- Быстрая разработка

**Плюсы:**

- Простая разработка и тестирование
- Нет распределённых транзакций

**Минусы:**

- Сложно масштабировать по компонентам
- Риск “спагетти-кода”
- Любой баг может повлиять на весь сервис

**Продакшен-пример:**

- Малый веб-сайт, админ-панель

---

## 2️⃣ **Microservices Architecture**

**Описание:**

- Система разделена на множество сервисов, каждый отвечает за свой **bounded context**
- Коммуникация через HTTP/gRPC или события

**Когда используют:**

- Большие системы с ростом функционала
- Независимое масштабирование

**Плюсы:**

- Горизонтальное масштабирование отдельных сервисов
- Каждый сервис можно деплоить отдельно
- Легко поддерживать SLA для ключевых сервисов

**Минусы:**

- Сложная интеграция
- Eventual consistency
- Требует DevOps, мониторинга и трассировки

**Продакшен-пример:**

- E-commerce: AuthService, OrderService, InventoryService, PaymentService, NotificationService

---

## 3️⃣ **Event-Driven Architecture (EDA)**

**Описание:**

- Сервисы взаимодействуют через **события**
- Используются брокеры сообщений (Kafka, RabbitMQ, NATS)
- События → асинхронная коммуникация

**Когда используют:**

- High-throughput системы
- Системы с асинхронными процессами
- Микросервисы, где важна loose coupling

**Плюсы:**

- Устойчивость к сбоям
- Лёгкое масштабирование
- Можно replay событий (audit/logging)

**Минусы:**

- Eventual consistency
- Сложнее отлаживать
- Нужно продуманное управление backpressure

**Продакшен-пример:**

- Система уведомлений: OrderCreated → InventoryService → PaymentService → EmailService → PushService

---

## 4️⃣ **CQRS + Event Sourcing**

**Описание:**

- CQRS: **Command Query Responsibility Segregation** → разделяем write и read модели
- Event Sourcing: все изменения → события, которые можно replay

**Когда используют:**

- Сложные бизнес-процессы
- Системы с высокой аналитикой / audit trail

**Плюсы:**

- Оптимизация read и write операций
- Возможность восстановления состояния системы из событий

**Минусы:**

- Сложнее реализовать
- Нужно хранение событий и проекции

**Продакшен-пример:**

- Финансовые системы: транзакции → события → read model для отчетности
- NestJS: микросервисы + Kafka + Postgres + Redis для read model

---

## 5️⃣ **Serverless / Function-as-a-Service**

**Описание:**

- Мелкие функции, выполняющиеся по событию (AWS Lambda, Azure Functions)
- Часто event-driven + микросервисы

**Когда используют:**

- Системы с непредсказуемой нагрузкой
- Триггерные задачи, cron jobs, webhook обработка

**Плюсы:**

- Платишь только за время выполнения
- Автоматическое масштабирование
- Нет управления серверами

**Минусы:**

- Cold start latency
- Ограничения по времени выполнения и ресурсам
- Сложнее отлаживать и тестировать

**Продакшен-пример:**

- Image processing → upload triggers Lambda
- Payment webhook → триггер на Lambda

---

## 6️⃣ **Layered / Modular Architecture**

**Описание:**

- Традиционное разделение на слои: Presentation → Application → Domain → Infrastructure

**Когда используют:**

- Чёткая граница ответственности
- Подходит для NestJS, где слои: Controller → Service → Repository → Entity

**Плюсы:**

- Читаемость кода
- Простое тестирование
- Легко разделять обязанности

**Минусы:**

- Может возникнуть излишняя связанность между слоями
- Не решает масштабируемость сама по себе

**Продакшен-пример:**

- NestJS API → Controllers → Services → Repositories → Postgres / Redis

---

## 🔹 Как выбирать паттерн в System Design

| Паттерн               | Когда                   | Пример в проде             |
| --------------------- | ----------------------- | -------------------------- |
| Monolith              | MVP / маленький проект  | Admin panel                |
| Microservices         | Большая система, SaaS   | E-commerce / CRM           |
| Event-Driven          | Асинхронные процессы    | Notifications / Analytics  |
| CQRS + Event Sourcing | Сложные бизнес-процессы | Banking / Finance          |
| Serverless            | Триггерные задачи       | Image processing, Webhooks |
| Layered               | Чёткая структура кода   | NestJS API                 |

---

## 🔹 На собесе точно спросят:

1. Почему выбрать microservices vs monolith?
2. Как организовать event-driven коммуникацию между сервисами?
3. Как масштабировать систему при миллионах запросов?
4. Как строить CQRS + Event Sourcing и почему это лучше обычных CRUD?
5. Trade-offs разных архитектур: latency, consistency, complexity
